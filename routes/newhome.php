<?php
use Illuminate\Support\Facades\Route;

/*Route::get('/', 'NewHomeController@index')
    ->middleware(['localeSessionRedirect', 'localizationRedirect'])
    ->prefix( LaravelLocalization::setLocale())
    ->name('index');*/ //moved to web
