<?php
/* if ( ! function_exists('set_provider'))
{    
    function set_provider(){
       $ci=get_instance();
       $provider_obj=new ProviderSession();
       $provider_obj->id="P001";
       $provider_obj->name="Provider";
       $ci->session->SetSession("provider", $provider_obj);
    }
} */